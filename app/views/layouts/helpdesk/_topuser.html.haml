.navbar
  .navbar-inner
    / .container
    =link_to t('helpdesk.name'), root_path, :class=>'brand', :title => t('helpdesk.name')
    %nav
      %ul.nav
        = menu_li t('helpdesk.tickets.title'),tickets_path
    